@model BlogBook.Model.Post

@{
    ViewData["Title"] = Model.Title;
}

<h1>Szczegóły</h1>

@await Html.PartialAsync("_PostDetailsPartial", Model)

<a class="btn btn-outline-secondary mt-5" asp-action="Index" asp-controller="/Posts">Powrót</a>
